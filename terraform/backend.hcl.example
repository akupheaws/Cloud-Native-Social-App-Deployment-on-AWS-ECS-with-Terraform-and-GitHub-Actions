# Used by GitHub Actions; values are injected at runtime.
bucket         = "<YOUR_TF_STATE_BUCKET>"
key            = "terraform.tfstate"
region         = "<YOUR_AWS_REGION>"
dynamodb_table = "<YOUR_TF_LOCK_TABLE>"
encrypt        = true
